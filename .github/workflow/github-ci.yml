name: Build and Deploy
on: # 监听 dev 分支上的 push 事件
  push:
    branches:
      - dev
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest # 构建环境使用 ubuntu
    steps:
      - name: Checkout
        uses: actions/checkout@v2.3.1  
        with:
          persist-credentials: false
      - name: Install and Build # 下载依赖 打包项目
        run: |
          npm install
          npm run build
      - name: Deploy # 将打包内容发布到 github page
        uses: JamesIves/github-pages-deploy-action@4.3.3 # 使用别人写好的 actions
        with:  # 自定义环境变量
          ACCESS_TOKEN: ${{ secrets.ACTIONS_TOKEN_SECRET }} # ACTIONS_TOKEN_SECRET 是我的 secret 名称，需要替换成你的
          branch: dev
          folder: dist
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v4
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - run: echo "🍏 This job's status is ${{ job.status }}."



      # - name: Deploy # 将打包内容发布到 github page
      #   uses: JamesIves/github-pages-deploy-action@3.5.9 # 使用别人写好的 actions
      #   with:  # 自定义环境变量
      #     ACCESS_TOKEN: ${{ secrets.ACTIONS_TOKEN_SECRET }} # ACTIONS_TOKEN_SECRET 是我的 secret 名称，需要替换成你的
      #     BRANCH: dev
      #     FOLDER: dist
      #     REPOSITORY_NAME: lycorisra/webui-standards-demo # 这是我的 github page 仓库
      #     TARGET_FOLDER: github-actions-demo # 打包的文件将放到静态服务器 github-actions-demo 目录下